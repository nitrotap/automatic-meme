import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'poetry-line-masher',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
